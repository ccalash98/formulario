window.addEventListener("load", function() {
    const loader = document.querySelector(".loader_bg");
    if (loader) {
        loader.style.display = "none"; // Oculta el loader cuando todo está cargado
    }
});
